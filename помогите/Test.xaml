<Window x:Class="помогите_.Test"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:помогите_"
        mc:Ignorable="d"
        Title="Test" Height="450" Width="800">

    <Grid>
        
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Label Content="Название теста:" Margin="0,0,589,26" Grid.RowSpan="2"/>
        <TextBox x:Name="testNameTextBox" Grid.Row="0" Margin="100,0,0,0" Width="200"/>

            <Label Content="Количество вопросов:" Grid.Row="1"/>
        <TextBox x:Name="numberOfQuestionsTextBox" Grid.Row="1" Margin="100,0,0,0" Width="100" />

            <Button Content="Добавить вопрос" Grid.Row="3" HorizontalAlignment="Left" Margin="10" Click="AddQuestion_Click"/>
            <Button Content="Сохранить тест" Grid.Row="3" HorizontalAlignment="Right" Margin="10" Click="SaveTest_Click"/>

        <ListBox x:Name="testListBox" Grid.Row="2" Margin="10" ItemsSource="{Binding Questions}"/>


    </Grid>

</Window>
